rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allows any authenticated user to read and write.
    // Ensure you secure this in production!
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
